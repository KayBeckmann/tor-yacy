# YaCy Tor Hidden Service Konfiguration
# =======================================

# Praefix fuer die .onion-Adresse (Vanity Address)
# Nur Kleinbuchstaben a-z und Ziffern 2-7 erlaubt (Base32)
# Je laenger der Praefix, desto laenger dauert die Generierung:
#   3 Zeichen: ~Sekunden
#   4 Zeichen: ~Minuten
#   5 Zeichen: ~Stunden
#   6 Zeichen: ~Tage
# Leer lassen fuer zufaellige Adresse
VANITY_PREFIX=

# Port ueber den YaCy im Tor-Netzwerk erreichbar sein soll (Web-UI)
ONION_PORT=80

# Port fuer YaCy Peering ueber Tor
# Standard ist 8090 (YaCy-Standard)
PEER_PORT=8090

# Lokaler Port fuer den Zugriff auf YaCy (Host)
# Ueber http://localhost:<PORT> erreichbar
YACY_LOCAL_PORT=8090

# Netzwerk-Name fuer das YaCy Tor-Netzwerk
# Alle Knoten im selben Netzwerk muessen den gleichen Namen verwenden
NETWORK_NAME=tor

# Bootstrap-Peer (.onion-Adresse eines bestehenden Knotens)
# Neue Knoten verbinden sich hierueber mit dem Netzwerk
# Beispiel: abcdefghijklmnopqrstuvwxyz234567abcdefghijklmnopqrst.onion
# Leer lassen fuer den ersten Knoten im Netzwerk
BOOTSTRAP_PEER=

# YaCy Admin-Zugangsdaten
# Benutzername fuer das Admin-Interface (Standard: admin)
YACY_ADMIN_USER=admin
# Passwort fuer das Admin-Interface
# WICHTIG: Unbedingt aendern vor dem produktiven Einsatz!
YACY_ADMIN_PASSWORD=
